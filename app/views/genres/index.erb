<!DOCTYPE html>
<html>

    <head></head>

    <body>

        <% @genres.each do |genre| %>
            <a href="/genres/<%= genre.slug %>"><%= genre.name %></a>
        <% end %>
        
    </body>

</html>